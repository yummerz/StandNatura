<UserControl x:Class="StandNatura.Views.HotspotMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:v="clr-namespace:StandNatura.Views"
             mc:Ignorable="d"
             Loaded="HotspotMapView_Loaded"
             Unloaded="HotspotMapView_Unloaded">

    <!-- The WebView2 is created in code-behind and injected into the shell's
         PageContent. Naming a WebView2 here directly would collide with
         AppShell's own XAML name scope (error MC3093). -->
    <v:AppShell x:Name="Shell" ActivePageKey="{Binding ActivePageKey}"/>
</UserControl>
