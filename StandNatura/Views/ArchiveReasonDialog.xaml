<Window x:Class="StandNatura.Views.ArchiveReasonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reason for Archive"
        Height="280" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1B4332">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Reason for Archive (Optional)"
                   FontSize="16" FontWeight="Bold"
                   Foreground="#74C69D"
                   FontFamily="Trebuchet MS"
                   Margin="0,0,0,5"/>

        <TextBlock Grid.Row="1"
                   Text="Provide an optional reason. The user will see this when they review their archived sighting."
                   FontSize="12"
                   Foreground="#95D5B2"
                   FontFamily="Trebuchet MS"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="2"
                 x:Name="ReasonTextBox"
                 Background="#2D6A4F"
                 Foreground="#FFFFFF"
                 BorderBrush="#52B788"
                 BorderThickness="2"
                 Padding="5,8,5,8"
                 FontSize="13"
                 FontFamily="Trebuchet MS"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"/>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Cancel"
                    Height="40"
                    Margin="0,0,10,0"
                    Background="#2D6A4F"
                    Foreground="#D8F3DC"
                    FontFamily="Trebuchet MS"
                    FontSize="13" FontWeight="Bold"
                    BorderBrush="#52B788" BorderThickness="2"
                    Click="Cancel_Click"/>

            <Button Grid.Column="1"
                    Content="Confirm Archive"
                    Height="40"
                    Background="#B7374A"
                    Foreground="#FFFFFF"
                    FontFamily="Trebuchet MS"
                    FontSize="13" FontWeight="Bold"
                    BorderBrush="#E05C6E" BorderThickness="2"
                    Click="Confirm_Click"/>
        </Grid>
    </Grid>
</Window>